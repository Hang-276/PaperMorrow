{
  "name": "@papermorrow/presentation-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, evidence-aware research presentation compiler for PaperMorrow",
  "type": "module",
  "bin": {
    "papermorrow-ppt": "./dist/src/cli.js"
  },
  "exports": {
    ".": "./dist/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate:paper": "npm run build --silent && node dist/src/cli.js fixtures/paper-report.json output/paper-report.pptx",
    "generate:progress": "npm run build --silent && node dist/src/cli.js fixtures/research-progress.json output/research-progress.pptx",
    "test": "npm run build --silent && node --test dist/tests/*.test.js",
    "test:runtime": "npm run build --silent && node --test dist/tests/*.test.js",
    "check": "npm run test"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "mathjax-full": "^3.2.2",
    "pptxgenjs": "^4.0.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
